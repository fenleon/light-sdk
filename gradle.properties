kotlin.code.style=official
android.useAndroidX=true
org.gradle.jvmargs=-Xmx4g
# Local memory cap: kotlin daemon hit 3.9 GB RSS and triggered system OOM (2026-08-08).
# Tune up if the build OOMs, down if freezes recur.
kotlin.daemon.jvmargs=-Xmx3g

# Local toolchain: JDK 21 runs Gradle, JDK 17 satisfies the plugin's toolchain request.
# Paths are machine-specific (see tools/ in the repo root); adjust if you move JDKs.
org.gradle.java.installations.paths=/home/fenn/Repo/light-phone/tools/jdk-21.0.12+8,/home/fenn/Repo/light-phone/tools/jdk-17.0.20+8

sdkGroup=com.thelightphone
sdkVersion=0.1.0
